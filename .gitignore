# Credentials & secrets — never commit these
credentials/
config/google-oauth-client.json

# Runtime data — logs, outputs, tokens
data/

# Alice personal config — gitignored (copy from *.example files in config/)
config/install-config.json
config/skill-personal.json

# Alice personal memory files — contain real identity/context data, gitignored
# Copy the *.example counterpart and fill in your own values.
memories/user_profile.md
memories/reference.md
memories/short_memory.md
memories/project.md

# Claude Code local settings & runtime files
.claude/settings.local.json
.claude/scheduled_tasks.lock
.mcp.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db
